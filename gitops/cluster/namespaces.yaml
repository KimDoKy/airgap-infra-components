# 환경별 네임스페이스(논리 분리). 클러스터 준비 시 1회 apply (또는 ArgoCD CreateNamespace=true 로 자동).
apiVersion: v1
kind: Namespace
metadata: { name: acme-app-dev, labels: { env: dev } }
---
apiVersion: v1
kind: Namespace
metadata: { name: acme-app-stg, labels: { env: stg } }
---
apiVersion: v1
kind: Namespace
metadata: { name: acme-app-prd, labels: { env: prd } }
