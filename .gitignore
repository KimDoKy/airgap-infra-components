# 실제 시크릿이 든 .env 는 절대 커밋하지 않음 (템플릿은 .env.example 사용)
.env
.env.bak
# VM 실행 명령 로그 (관리자 비번 평문 포함)
vm-commands.txt
# Claude Code 로컬 상태
.claude/
# node 빌드 산출물
node_modules/
# 컨테이너 이미지 tar / 오프라인 플러그인 (빌드 산출물 — 커밋 금지)
*.tar
*.tar.gz
*.tgz
*.hpi

# NCR 자격(레지스트리/access/secret) — 커밋 금지
.ncr
Jenkins/.ncr
